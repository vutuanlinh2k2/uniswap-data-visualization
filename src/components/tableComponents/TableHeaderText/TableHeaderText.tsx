import React from "react"

const TableHeaderText = ({ headerTitle }: { headerTitle: string }) => {
  return <p className="text-right w-full text-grey-primary">{headerTitle}</p>
}

export default TableHeaderText
